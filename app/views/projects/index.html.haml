- sidebar_blocks[:projects] = true
- block 'Projects' do
  %table.table
    %tr
      %th.first Name
      %th       Last error
      %th.last  Count
    = render projects

- actions do |link|
  - link.to 'New project', new_project_path